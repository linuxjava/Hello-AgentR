# Frontend Admin / Console UX

Web 管理端（`frontend-admin`）的屏态与交互边界；身份实体与能力矩阵以后端 [`docs/backend/CONTEXT.md`](../backend/CONTEXT.md) 为准，本上下文不重复定义。

## Language

**Admin Shell（管理壳层）**：
已登录后的全局框架（导航 + 顶栏身份区 + 内容区）；承载首页与账号管理等受保护页面。
_Avoid_: Layout（泛称时）、Dashboard（勿暗示本阶段有仪表盘业务）

**Login Page（登录页）**：
匿名访问的管理端入口；提交用户名与密码换取会话 token。
_Avoid_: 注册页（本阶段无自助注册）

**Home Placeholder（首页占位）**：
已登录可访问的落地页；本阶段无业务内容，仅占位。
_Avoid_: Overview、工作台（口语可，文档用首页占位）

**AdminUser List（账号列表）**：
分页展示 AdminUser 的管理页；支持用户名模糊与角色精确筛选。
_Avoid_: 用户管理（易与 EndUser 混淆）、Operator 列表

## Screens（本阶段 V0.1）

| # | 屏 / 态 | 谁能进 |
| --- | --- | --- |
| S1 | 登录页 | 匿名 |
| S2 | 管理壳层 | 已登录 |
| S3 | 首页占位 | 已登录 |
| S4 | 账号列表 | Admin / Staff |
| S5 | 创建账号 | 仅 Admin |
| S6 | 重置他人密码 | 仅 Admin |
| S7 | 变更角色 | 仅 Admin |
| S8 | 删除确认 | 仅 Admin |
| S9 | 修改自己的密码 | Admin / Staff |

## Navigation & Entry（本阶段）

- **侧栏**：首页、账号管理（仅两项）。
- **账号治理写操作**（创建 / 重置密码 / 改角色 / 删除）：挂在账号列表的行内或页头操作上，不单独占侧栏。
- **顶栏身份区**：展示 username + 角色；菜单含「修改密码」「登出」。
- **登录成功默认落地**：首页占位。

## Capability UI（本阶段）

- **Staff**：创建 / 重置密码 / 改角色 / 删除等无权限入口 **可见但禁用（灰显）**，附 tooltip 说明无权限；布局与 Admin 一致。
- **Admin**：对明显不可操作目标（Bootstrap Admin、当前登录自己）**禁用并附简短原因**；其余保护规则（末账号 / 末 Admin 等）提交后以后端文案（如 `A001009`）提示。
- 列表 / 筛选 / 改自己密码 / 登出：Admin 与 Staff 均可正常使用。

## Session UX（本阶段）

- Token 存 **localStorage**；应用启动时带 token 调用 `/admin/auth/me` 校验；成功进入壳层，失败清除本地会话并进入登录页。
- 受保护 API 返回未登录（如 `A000001`）→ 清除本地会话 → 跳转登录页。
- 已持有有效会话时访问登录页 → 自动进入首页占位。
- 登录页支持 **记住用户名**（仅缓存 username，不缓存密码）；与 token 持久化独立。

## Interaction Patterns（本阶段）

- **创建 / 重置他人密码 / 变更角色 / 删除 / 修改自己的密码**：均为挂在当前页上的 **模态框**（无独立详情路由）。
- **删除**：确认弹窗，文案点明物理删除、不可恢复。
- **修改自己的密码成功**：清除本地会话 → 提示需重新登录 → 跳转登录页（若已记住用户名则预填）。

## Feedback UX（本阶段）

- **字段校验**：表单内联错误；规则对齐后端（用户名 4–32、`[a-zA-Z0-9_]`；密码 8–64、须含字母与数字）。
- **业务失败**：优先展示后端 `message`；登录页用页内错误条，弹窗操作用弹窗内错误。
- **业务成功**（创建 / 重置 / 改角色 / 删除）：Toast 短提示，关闭弹窗并刷新列表。
- **全局意外**（网络 / 未知 5xx）：Toast 错误。

## Visual（本阶段）

- **本阶段不交付视觉定稿**；视觉后续用 Pencil 设计后再落地。
- 需求澄清与实现可先按功能闭环推进；样式以可替换为前提，不绑定玻璃拟态等试验风。

## Out-of-scope（本阶段明确不做）

- 启用 / 停用 UI
- 自定义角色 / 权限码配置 UI
- 登录验证码、失败锁定 UI
- 昵称 / 头像 / 邮箱、操作审计日志页
- 强制首次登录改密流程
- RAG 业务页面（文档、检索、会话等）
- **视觉设计定稿 / 设计系统落地**（交 Pencil 后续迭代）
